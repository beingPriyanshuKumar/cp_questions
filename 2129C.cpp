#include <bits/stdc++.h>
using namespace std ;
#define int long long
signed main(){
   int t;
    if (cin >> t) {
        while (t--) {
            int n,h,k;
            cin >> n >> h >> k ;
            int mag[n];
            for(int i=0;i<n;i++){
                cin >> mag[i];

            } 
            
            
        }
    }
    return 0;
    
}